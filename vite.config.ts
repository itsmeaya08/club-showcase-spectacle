// @lovable.dev/vite-tanstack-config supplies the TanStack, React, Tailwind,\n// path-alias, and Nitro integrations used by this project.\nimport { defineConfig } from "@lovable.dev/vite-tanstack-config";\n\nconst isGitHubPages = process.env.GITHUB_ACTIONS === "true";\n\nexport default defineConfig({\n  nitro: { preset: "vercel" },\n  tanstackStart: {\n    server: { entry: "server" },\n    prerender: { enabled: true, crawlLinks: true },\n  },\n  vite: {\n    base: isGitHubPages ? "/club-showcase-spectacle/" : "/",\n  },\n});\n